version: '3.8'

services:
  mcp-storage:
    build: .
    container_name: mcp-storage-go
    ports:
      - "5435:5435"
    env_file:
      - .env.docker
    environment:
      - PORT=5435
      - HOST=0.0.0.0
      - DEBUG=1
      - LOG_LEVEL=debug
    restart: unless-stopped